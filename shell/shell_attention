添加用户test并建立/HOME目录：useradd -m test

新建文件默认权限值：666
新建目录默认权限值：777

umask的取值视系统而定通常可在/etc/pofile文件下进行修改

root用户默认提示符为#，普通用户默认提示符为$.可通过/etc/profile文件修改

字符串长度：${!string}

脚本的最后一个参数：${!#}

id -gn 组名
id -un 用户名
$UID   用户ID号

ATTENTION:改变文件属组时，文件属主必须是目标组的成员，或者以root账户进行修改
